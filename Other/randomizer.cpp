#include <iostream> 
#include <vector> 
#include <string>
using namespace std; 

int main() 
{

	vector<string> editorial; 
	vector<string> exec; 
	//Need a way to do it randomly
	//Two random numbers (one for exec & one for editorial) 
	//I need to remove it from the vector 
	//I should probably make a class (the values would be their section & partner)
	
	cout << "Time to pick partners: " << endl; 
	
	while(editorial.size() != 0 && exec.size() != 0)
	{
		
		
	}
	
	

}

